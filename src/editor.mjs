import {clone,canonical,digest,LudotapeError} from './index.mjs';
export function createDraft(initial={}){let document=clone(initial),revision=0;const history=[];return {get document(){return clone(document)},get revision(){return revision},get dirty(){return history.length>0},replace(next,label='replace'){canonical(next);history.push({document,revision,label});document=clone(next);revision++;return this.document},update(mutator,label='update'){if(typeof mutator!=='function')throw new LudotapeError('E_EDITOR','mutator required');const next=mutator(clone(document));return this.replace(next,label)},undo(){const old=history.pop();if(!old)return false;document=old.document;revision++;return true},snapshot(){return {format:'ludotape/draft@1',revision,document:clone(document),digest:digest(document)}},markSaved(){history.length=0}}}
