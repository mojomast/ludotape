import {clone,canonical,digest,deepFreeze,LudotapeError} from './index.mjs';
export function createDraft(initial={}){let document=clone(initial),revision=0;const history=[];return Object.freeze({get document(){return clone(document)},get revision(){return revision},get dirty(){return history.length>0},replace(next,label='replace'){canonical(next);history.push({document,revision,label:String(label)});document=clone(next);revision++;return clone(document)},update(mutator,label='update'){if(typeof mutator!=='function')throw new LudotapeError('E_EDITOR','mutator required');return this.replace(mutator(clone(document)),label)},undo(){const old=history.pop();if(!old)return false;document=old.document;revision++;return true},snapshot(){return deepFreeze({format:'ludotape/draft@1',revision,document:clone(document),digest:digest(document)})},markSaved(){history.length=0}})}
