import {mkdir,cp,rm} from 'node:fs/promises';const root=new URL('../',import.meta.url),dist=new URL('../dist/',import.meta.url);await rm(dist,{recursive:true,force:true});await mkdir(dist,{recursive:true});for(const name of ['src','examples','studio'])await cp(new URL(`../${name}/`,import.meta.url),new URL(`../dist/${name}/`,import.meta.url),{recursive:true});console.log('Built dist/ with src, examples, and studio assets.');
