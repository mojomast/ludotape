// Shared constants used by devkit templates. Keep template outputs deterministic and
// consistent with each other (e.g. core.mjs and core-manifest.mjs must agree on version).
export const CORE_TEMPLATE_VERSION = '0.1.0';
export const GAME_TEMPLATE_VERSION = '1.0.0';
